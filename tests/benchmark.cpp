#include "preprocess.h"

#include <cstdio>

int main(int argc, char* argv[]) {
    // TODO: load mesh from argv[1]
    // Run each stage with CUDA event timing
    // Compare against CPU reference implementations
    // Print timing results table
    printf("benchmark: not yet implemented\n");
    return 0;
}
